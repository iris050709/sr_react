import ResetPassword from '../components/ResetPassword';
import FormularioLogin from '../components/ResetPassword';

const ResetPassword = () => {
    return (
        <div>
        <ResetPassword />
        </div>
    );
};

export default ResetPassword;
