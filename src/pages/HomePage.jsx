import Navbar from '../components/Navbar';

const HomePage = () => {
    return (
        <div>
        <Navbar />
        <h1>Bienvenido a la página principal</h1>
        </div>
    );
};

export default HomePage;
